// GlobalStyles is now just a wrapper that imports the CSS file
// This maintains compatibility with existing imports
import './index.css'

// Export a no-op component for compatibility
const GlobalStyles = () => null

export default GlobalStyles
